$(function(){
    // Init
   $( '#map' ).mapLite();

   $( '#stationDetail' ).drawerPanel({
       state: 'closed',
       position: 'right',
       width: 400,
       color: '#fee'
   });

    // 
});